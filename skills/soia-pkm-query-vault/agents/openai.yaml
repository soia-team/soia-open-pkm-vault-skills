interface:
  display_name: "Query Vault"
  short_description: "只读搜索 vault，并区分当前、稳定、证据与历史来源"
  default_prompt: "Use $soia-pkm-query-vault to answer this question from my vault with cited paths, without changing any files."
