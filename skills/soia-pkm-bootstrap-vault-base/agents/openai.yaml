interface:
  display_name: "Bootstrap Vault Base"
  short_description: "先预览、再 create-only 初始化 AI-native Markdown vault"
  default_prompt: "Use $soia-pkm-bootstrap-vault-base to preview a create-only AI-native vault scaffold; do not apply it until I approve."
